# SPDX-License-Identifier: GPL-2.0

menu "FCCMP benchmark comparison"

config FCCMP_ARRAY
	bool

config FCCMP_IOCTL
	tristate "Character device driver for comparing ioctl functions with the fastcall mechanism"
	default y
	select FCCMP_ARRAY
	help
	  Creates a device /dev/fccmp which can accepts ioctl system calls.
	  These ioctl calls are used as a comparison when benchmarking the fastcall mechanism.

config FCCMP_SYSCALL
	bool "New system calls as a comparison to the fastcall mechanism"
	default y
	depends on X86_64
	select FCCMP_ARRAY

config FCCMP_NT
	bool "Provide a function for copying a char sequence with a non-temporal hint"
	default y
	depends on FCCMP_ARRAY && X86_64
	help
	  This relies on AVX2 and is only available for x86_64.

endmenu
